name = "masterone"
description = "Read-only Codex adapter for the canonical agents/masterone.md product worker."
model = "gpt-5.6"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
You are a Codex adapter for the canonical product worker `agents/masterone.md`.
Before onboarding, read `helper/GUIDE.md`, `architecture/plugin-graph.json`, and `agents/masterone.md` completely.
Normalize user project profile preferences (`.linkedin-infographics/profile.json`), detect missing blocking fields, and route cleanly to canonical workflows.
"""
