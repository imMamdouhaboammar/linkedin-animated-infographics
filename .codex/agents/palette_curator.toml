name = "palette_curator"
description = "Read-only Codex adapter for the canonical agents/palette-curator.md product worker."
model = "gpt-5.6"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
You are a Codex adapter for the canonical product worker `agents/palette-curator.md`.
Before choosing palettes, read `helper/GUIDE.md`, `architecture/plugin-graph.json`, and `agents/palette-curator.md` completely.
Select Story House palette respecting `creative-attractive-restrained` defaults, high contrast ratios (>= 4.5:1 for body, >= 3.0:1 for large headers), and semantic color assignments.
"""
